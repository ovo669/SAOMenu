# SAOMenu 模组更新说明

## 2026-09-03(14):死亡碎裂碎片错误贴图修复

- 症状:碎片上偶尔出现彩格/方块刻纹/物品图案等错误贴图
- 根因:粒子贴图集渗色——碎片带自旋,原版对自旋粒子是轴对齐 UV 采样,
  pickSprite 给的 UV 贴着图集图块边缘,双线性过滤 + mipmap 把
  <strong>相邻图块</strong>(原版粒子/物品)的颜色渗到碎片边缘
- 修复:覆写 getU0/getU1/getV0/getV1,每边向图块中心收进 1/16,
  彻底隔断对相邻图块的采样(视觉上碎片图案略微放大,不可感知)


## 2026-09-03(13):自由观察相机居中修复

- 症状:Alt 自由观察时相机不在人物身上,中心偏离角色
- 根因:手算世界向量喂给 Camera.move——move 接收的是相机<strong>局部空间</strong>
  坐标,世界向量经相机旋转矩阵二次变换后被甩到偏离位置
- 修复:放弃手动摆相机,改为 Redirect 原版 setup 内部两个调用——
  setRotation(初始旋转)替换为自由相机角度、getMaxZoom(4.0)替换为滚轮距离;
  相机由原版自己的「眼部锚点 + 沿视角后退」逻辑摆放,恒以玩家为圆心环绕,
  穿墙收紧也由原版处理

## 2026-09-03(12):黑色背板移除 + Alt 自由观察解耦重做

### 1. 第三人称菜单板:黑色板块彻底移除
- 不再绘制任何深色背板——板上只贴「玩家屏幕所见」的不透明画面
  (世界+菜单合成结果),所见即所得,不可能再出现黑色板块

### 2. Alt 自由观察:相机与角色朝向解耦(重做)
- 之前缺陷:转鼠标仍会转角色朝向;中键锁定失效;松开 Alt 回第一人称
- 重做方案:
  - MouseHandlerMixin 在 turnPlayer 头/尾注入:把鼠标转向增量从玩家
    朝向<strong>转存</strong>到自由相机(yaw/pitch),玩家朝向当场回滚——
    角色永远不转,相机自由转
  - CameraMixin 在 Camera.setup 尾部用自由相机角度重设相机旋转与位置
    (setRotation + move,自带穿墙收紧),距离由滚轮控制(1.5~12 格)
  - 中键锁定改为 tick 轮询边沿检测(GLFW 轮询,不依赖事件注入);
    锁定后松开 Alt 保持当前视角,再按中键解锁
  - 松开 Alt(未锁定)恢复进入前的相机类型,镜头弹回玩家朝向

## 2026-09-03(11):黑板终极修复(不透明贴图) + Alt 自由观察

### 1. 第三人称菜单板黑板再修
- 板纹理改为<strong>不透明绘制</strong>:blit 拷贝的主帧缓冲 alpha 在部分环境
  (OptiFine 等)为 0,开混合时菜单 RGB 会被 0 alpha 混掉只剩深色背板;
  现在直接不透明贴出(纹理 RGB 就是玩家所见的完整合成画面),
  对 alpha/剔除状态完全免疫;开合淡入用 RGB 缩放近似(不可感知)

### 2. Alt 自由观察(新功能)
- 按住 Alt:临时切第三人称自由观察,移动鼠标转视角,松开自动恢复
  原视角与朝向(镜头弹回,不改角色实际朝向)
- 按住 Alt 期间按<strong>鼠标中键</strong>:锁定/解锁当前视角(锁定后松开 Alt 仍保持)
- 自由观察期间<strong>滚轮</strong>:拉远/拉近相机(1.5~12 格,默认 4 格)
- 实现:SAOFreeLook 状态机(GLFW 轮询 Alt)+ CameraMixin ModifyConstant
  (替换原版硬编码第三人称距离 4.0)+ MouseHandlerMixin(中键/滚轮截获);
  双平台 mixin 已注册


## 2026-09-03(10):第三人称菜单板显式双面渲染(修黑板)

- 症状:背后第三人称(F5 第一档)菜单板只剩深色板、菜单内容不见
- 根因:菜单板四边形未显式关闭背面剔除,渲染器残留的 cull 状态因环境
  而异(预览环境恰好被 3D 血条的 NO_CULL 状态顺带关掉,OptiFine 环境
  则开着剔除)——板面被剔除后只剩背板
- 修复:绘制板时显式 disableCull + enableDepthTest + LEQUAL,画完恢复;
  两个第三人称视角(含前置)都能看到菜单内容

## 2026-09-03(9):第三人称菜单板改用主帧缓冲 blit(黑板 bug 修复)

- 症状:第三人称下面板只剩深色背板(菜单内容不显示)
- 原方案(手搓离屏 FBO + 正交投影渲染菜单)在该环境 GL 状态下无法写入,
  经洋红探针/矩阵日志/glReadPixels 逐层排查定位
- 新方案:菜单屏渲染完成后直接 glBlitFramebuffer 拷贝主帧缓冲到备用纹理
  (GPU→GPU),世界板贴图——内容与玩家所见像素级一致且零自建管线
- 菜单内支持 F5 切换视角(原版 Screen 打开时不处理视角键)

## 2026-09-03(8):物品栏圆点重排/缩放 + 第三人称菜单板

### 1. 底部圆点物品栏重排 + 大小可调
- **副手移到第一格**: 第 1 个圆点是副手,与主栏 9 格之间隔开一档
  (间隔 = 0.55×圆点直径),整组仍水平居中
- **大小可调**: 新配置 `hotbarScale`(0.6-1.6),设置 → 界面 → "物品栏大小"滑块,
  缩放时底缘锚定(圆点变大自然向上长,不越过屏幕底)
- 点击/悬停提示同步新布局(第 1 格悬停显示副手物品)

### 2. 第三人称菜单板(世界空间,主帧缓冲 blit)
- 打开菜单时,角色面前 1.35 格浮现一块 1.62 格高的菜单板,F5 第三人称可见;
  **内容 = 真实的 SAOMenuScreen 离屏渲染**(RenderTarget),与第一人称
  HUD 菜单一模一样——字体/图标/圆按钮/菜单项/开合动画/悬停高亮全部同源
- 实现方式:菜单屏渲染完成后(Forge ScreenEvent.Render.Post /
  Fabric ScreenEvents.afterRender)用 glBlitFramebuffer 把主帧缓冲
  GPU 拷贝到备用纹理,世界渲染阶段把纹理贴到角色面前的板上
  (UV 裁掉底部圆点栏)——不搭离屏管线,与玩家所见像素级一致;
  板锚定玩家身体朝向,随菜单开合缩放弹出、待机时上下轻浮
- 第一人称不渲染(全屏 HUD 接管,且面板贴脸会被透视放大);
  设置 → 界面 → "第三人称菜单"可开关(默认开)
- **纯本地渲染**:同客户端第三人称可见;其他真实玩家看到需要额外的
  网络同步(暂未实现,后续可加广播包)
- 实现 `SAOMenu3DPanel`(common),挂入双平台 AFTER_ENTITIES 世界渲染钩子;
  深色背板兜底,光影客户端上纹理被当不透明时观感仍正确

## 2026-09-03(7):红点简化为纯色圆点

- 红点数据源修复已验证生效,应用户要求去掉脉冲/白环/朝向线等装饰,
  只保留一个实心 SAO 红点(自身稍大、队友稍小);诊断日志一并移除

## 2026-09-03(6):地图面板可读性整改(地形对比 + 红点脉冲/朝向)

用户实测反馈面板"看不见自己的位置",复盘为观感问题叠加:
蓝青配色把地形色压得与「未探索底色」几乎一致,整张图像空白,
即使红点画对了也不显眼。

- **地形对比度重做**: 未探索底色改为近黑深蓝(原与地形色过于接近),
  已探索地形整体提亮提饱和——草地/道路/水在深底上一眼可辨
- **自身红点强化**: 加 ~1.4Hz 脉冲呼吸(旧版是恒定小红十字,
  在蓝色地图上不显眼),并按玩家朝向画白色短向线
  (地图上方=北,与原版一致);队友红点保持小号恒亮
- 坐标换算沿用上一版修复(像素 = 世界偏移/每像素格数 + 64)

## 2026-09-03(5):修复地图红点定位与血条环疯转

### 1. 地图红点不显示/位置错误
- **原因**: `SAOMapPanel.worldToPixel` 漏掉了原版地图装饰坐标→绘制像素的
  ÷2 换算(装饰坐标是「半像素」单位),红点位置整体放大两倍,
  几乎永远被钳在地图边缘或被判为越界而不显示
- **修复**: 像素 = 世界偏移/每像素格数 + 64,与原版装饰渲染一致,
  自身红点与队友红点都恢复正确落点

### 2. 生物血条环转动太快
- **原因**: 环绕弧带直接锚定生物 `yBodyRot`,而怪物 AI 每 tick 都在
  调整身体朝向(游荡/追踪/被打转向),环带跟着疯转
- **修复**: 弧带朝向改为限速阻尼——每 tick 最多转 2.5°
  (`ROT_SPEED_DEG_PER_TICK`),沿最短方向缓慢追赶生物朝向;
  小幅抖动被滤平,生物转身时环带优雅地缓缓转过去。
  转动逻辑抽为纯函数 `approachAngle` 并补 1 项单元测试(共 81 项)

## 2026-09-03(4):亚斯娜主题正播终点改为她的脸特写

- 点击分类进入亚斯娜(粉白)主题页时,背景从桐人眼睛特写(帧 75)
  正播的终点由片尾(帧 155)改为**亚斯娜脸特写**(视频 t≈10s → 帧 150)
  后停住;返回仍从当前帧倒放回桐人眼睛特写(帧 75)
- 两个定格点首尾呼应:蓝白页停在桐人眼睛、粉白页停在亚斯娜笑脸

## 2026-09-03(3):开场定格眼睛特写 + 桐人/亚斯娜双主题 + 减少遮挡

- **开场定格**: 打开设置界面时背景 4 倍速从首帧扫入桐人眼睛特写
  (视频 t≈5s → 帧 75)后定格;点击分类从定格处继续正播到片尾
  (进入亚斯娜紫色章节),点击返回从当前帧倒放回眼睛特写停住
- **桐人/亚斯娜双主题**: 根页面为蓝白系(冰蓝 #4FA8E8 + 白,桐谷和人),
  点击分类进入设置页后整体切换为粉白系(樱粉 #FF8AB0 + 白,亚斯娜),
  返回时切回蓝白;斜切转场色带颜色随目标页主题
  (与背景 0-6s 蓝色章节 / 6.5s 起紫色章节的内容变化天然同步)
- **减少遮挡**:
  - 全屏暗色渐变叠加大幅减淡(顶部 12%、底部 38%,原为 33%/65%)
  - 根页面分类改为阶梯排布的条目式:平时只有竖色条 + 大字 + 编号,
    不画底板,悬停时半透明斜板才展开
  - 设置行底板透明度减半(0.62→0.34,悬停 0.72),底部按钮同减

## 2026-09-03(2):背景播放节奏改为"播一段 → 点按钮重播 → 返回倒放"

- 背景从 ping-pong 循环改为播放状态机:
  - **打开设置界面**:从首帧正向播一段(约 10 秒),播完停在尾帧
  - **点击分类按钮**:背景从首帧重新播放一段(配合斜切转场)
  - **点击返回**:背景从当前帧倒放回首帧(磁带回带效果),停在首帧
- 实现:`SAOSettingsScreen` 新增 `BgMode(FORWARD/REVERSE)` 状态机,
  帧号由模式起始帧 + 经过时间推导,端点自动钳制停住,无需额外停帧状态

## 2026-09-03:P5 风格设置界面 + 视频背景

### 1. 女神异闻录风格设置界面重做
- **新文件**: `SAOSettingsScreen.java`(替代旧 `SAOConfigScreen`,旧类保留未删)
- **视频动画背景**: 「设置背景.mp4」(3840x2160@60fps)经 `tools/gen_settings_bg.py`
  抽帧(480x270@15fps,共 156 帧)拼成 7680x2700 帧贴图集
  `textures/gui/settings_bg.png`(仅 2MB),运行时 ping-pong 正反循环播放,
  全屏 cover 拉伸 + 暗色渐变叠加保证文字可读
- **P5 式交互**:
  - 根页面:4 个斜切分类板(布局 / 战斗 / 界面 / 主题)从左侧级联滑入(带过冲),
    悬停时板体右移、填充主题色、文字反色,右侧出现巨型编号水印
  - 点击分类:选中板加速飞出左侧、其余板飞出右侧,随后主题色斜切色带
    (黑带+主题色主带+前缘白条)扫过全屏,色带移开时设置行从右侧
    带过冲逐条弹出 —— 即"点击按钮播放动画 → 按钮位置改变 → 弹出新选项"
  - 返回:反向转场,设置行飞出、分类板从左侧滑回
  - 装饰:缓慢横移的斜切细线、斜切菱形滑块旋钮/开关指示、页面标题主题色竖条
- **设置项分四页管理**(内容与旧界面等价并补齐缺项):
  - 布局:锚点 X/Y、菜单缩放、浮动幅度、跟随鼠标(新)、隐藏原版快捷栏
  - 战斗:SAO HUD、皮肤头像、目标血条、伤害数字、死亡碎裂、碎裂密度
  - 界面:时钟、时钟大小(新)、24 小时制、显示日期、入世欢迎动画、成就通知、音效
  - 主题:主题色滑块 + SAO 橙 / ALO 蓝 / GGO 红预设
- 底部常驻"恢复默认 / 完成";修改即时生效并持久化到 `config/saomenu.json`

### 2. 修复
- **菜单从子界面返回即消失**: 菜单 beginClose 动画未走完时被挂起
  (如打开设置),返回后 `closeP>=1` 第一帧即被置空——现在 `init()`
  检测到 closing 状态会复位并重播开启动画
- **无头自检(preview)连续中断**: 装备二级菜单区段起约 30 处
  `client.screen.*` 裸调用在界面意外关闭时抛 NPE 崩溃——统一换成
  `clickScreen/moveScreen/closeScreen` 等空检查 helper;新增
  `settings.png` 截图覆盖新设置界面;O 键层级返回时序修正
  (设置→菜单→游戏),并修掉 tick 分支两处笔误

### 3. 资源
- 新增 `assets/saomenu/textures/gui/settings_bg.png`(2.0MB 帧贴图集)
- 语言文件 zh/en/ja/ko 各新增 12 键(设置界面标题/分类/返回/跟随鼠标),
  并补齐 ja/ko 此前缺失的 config 键

### 4. 已知问题(9/2 遗留,与本此改动无关)
- 菜单"装备"二级展开的点击坐标与 9/2 的 MenuLayout 几何改动不匹配,
  自检中点击二级项会关闭菜单(`menu_child.png` 等三张截图缺失),
  待单独排查 `MenuLayout.childItemRect` / `menuItemRect`

## 构建说明

```bash
cd D:\模组\SAOMenu
.\gradlew.bat build
```

编译后的 JAR 文件位于(dist/ 已同步):
- `dist/saomenu-forge-1.0.0.jar` → Forge 47.3.0+(需 Architectury API,已内嵌)
- `dist/saomenu-fabric-1.0.0.jar` → Fabric Loader 0.15+(需 Fabric API)

重新生成视频背景贴图集:

```bash
python tools/gen_settings_bg.py "D:\模组\SAOUI\设置背景.mp4"
```
